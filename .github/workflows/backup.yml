name: Update Blog Stats and Backup Articles

on:
  issues:
    types: [opened, edited, deleted, closed, reopened]
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'backup/**'
      - '.keep-alive'
  schedule:
    - cron: '0 0 * * *' # 每天 UTC 0点自动运行
  workflow_dispatch: # 允许手动触发

permissions:
  contents: write

jobs:
  update-and-backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      # --- 步骤 1: 备份文章 ---
      - name: Fetch All Issues and Backup
        run: |
          mkdir -p backup
          curl -s "https://api.github.com/repos/${{ github.repository }}/issues?state=all&per_page=100" | jq -c '.[]' | while read -r issue; do
            if echo "$issue" | jq -e '.pull_request' > /dev/null; then
                continue
            fi
            TITLE=$(echo "$issue" | jq -r '.title' | sed 's/[/\\:?*"<>|]/-/g')
            BODY=$(echo "$issue" | jq -r '.body')
            if [ "$BODY" != "null" ]; then
              echo "$BODY" > "backup/${TITLE}.md"
            fi
          done

      # --- 步骤 2: 更新 README 统计 (JavaScript) ---
      - name: Update README Content
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            
            const { data: issues } = await github.rest.issues.listForRepo({
              owner,
              repo,
              creator: owner,
              state: 'open'
            });
            
            const posts = issues.filter(i => !i.pull_request);
            const count = posts.length;
            const lastUpdate = count > 0 ? posts[0].created_at.split('T')[0] : 'N/A';
            
            const content = `
            <div align="center">
              <img src="https://avatars.githubusercontent.com/u/64282354?v=4" width="100" style="border-radius:50%">
              <h1>Hex Blog</h1>
              <p><b>是生活玩弄了你，还是你享受了生活！</b></p>
              <p>📝 文章篇数：<b>${count}</b> &nbsp;&nbsp; 📅 最近更新：<b>${lastUpdate}</b></p>
            </div>
            
            ---
            `;
            
            const readme = fs.readFileSync('README.md', 'utf8');
            // 替换 README 开头到第一个 --- 之间的内容
            const newReadme = readme.replace(/[\s\S]*?(?=---|$)/, `\n${content}\n`);
            fs.writeFileSync('README.md', newReadme);

      # --- 步骤 3: 统一提交、推送并处理保活 ---
      - name: Commit and Push
        run: |
          git config --global core.quotepath false
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add README.md backup/
          
          # 检查是否有内容变化
          if git diff --staged --quiet; then
            # 如果是定时任务触发且没有内容变化，执行保活提交
            if [ "${{ github.event_name }}" = "schedule" ]; then
              echo "触发保活机制：更新 .keep-alive 文件"
              date > .keep-alive
              git add .keep-alive
              git commit -m "chore: keep-alive (prevent action suspension) [skip ci]"
              git push origin main
            else
              echo "无内容更新，跳过推送。"
            fi
          else
            # 有内容更新，正常提交
            git commit -m "chore: automated backup and readme update [skip ci]"
            git pull --rebase origin main
            git push origin main
          fi
